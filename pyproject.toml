[project]
name = "tasktrackapi"
version = "0.1.0"
description = "An API for creating tasks with a description and marking them done when completed."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "pydantic>=2.13.4",
]
